#include <stdio.h>
int get_the_input(int a, int b )
{ 
    printf ("Enter  the value of a and b :\n");
    scanf("%d %d",&a,&b);
 }
 void print_the_numbers(int n){
      int a,b;
    for (int i=0;i>=n;i>20);
    int add = a+b;
    printf(" addition is :",add);
    int sub = a-b;
    printf("subraction is :",sub);
    int  multiply = a*b;
    printf("multiple is :",multiply);
 }
 int main(){
  int a,b,n;
  get_the_input(a,b);
  print_the_numbers(n);
   return 0;
 }